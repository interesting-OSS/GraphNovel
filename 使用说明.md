# LangNovel Studio — AI 长篇小说创作平台 使用说明

## 一、项目简介

LangNovel Studio 是一个基于多 AI 模型协作的长篇小说创作工具。通过 LangGraph 编排多个 AI Agent（写手、编辑、分析师、审稿人、逻辑检查、文笔检查、节奏检查），从世界观构建到逐章写作，全流程辅助作者完成长篇小说创作。

**技术栈：** React 19 + FastAPI + LangGraph + PostgreSQL + ChromaDB + Redis

---

## 二、快速启动

```bash
# 启动所有服务（首次需 build）
docker compose build
docker compose up -d

# 服务列表：
#   - 前端：http://localhost:3000
#   - 后端 API：http://localhost:8000
#   - 数据库：localhost:5432
#   - Redis：localhost:6379
```

---

## 三、使用流程

### 第 1 步：创建小说项目

1. 打开 `http://localhost:3000`，进入项目列表页
2. 点击 **「创建新项目」**，进入 3 步向导：
   - **基本信息**：输入书名 + 一句话简介
   - **类型风格**：选择小说类型（玄幻/都市/科幻等）
   - **创作设定**：选择叙述视角、目标字数、大纲模式
3. 点击 **「创建项目」**，自动跳转到项目工作台

### 第 2 步：世界观构建

进入侧边栏 **「世界观构建」**，AI 将按顺序生成：
- **时代背景** → **地理环境** → **力量体系** → **势力分布** → **文化设定** → **世界规则**
- 最后自动进行一致性校验，标记矛盾点供人工审核

> 如果某维度不满意，可以手动编辑后重新生成。

### 第 3 步：大纲规划

进入侧边栏 **「大纲」**，AI 根据世界观和角色概要：
- 自动规划分卷 / 分章结构
- 为每章生成标题、摘要、关键情节点
- 支持两种模式：
  - **传统模式 (1对1)**：每个大纲节点 = 1 章
  - **细化模式 (1对N)**：1 个大纲节点可展开为多章

> 大纲可以拖拽排序、手动增删改、或逐条展开为详细场景。

### 第 4 步：职业体系 & 组织势力

- **「职业体系」**：AI 为你的世界观设计 1-3 条职业/等级路线（如修仙境界、职业路径），每职业 5-10 个等级
- **「组织/势力」**：AI 生成 3-6 个势力，包含层级体系、首领、成员、立场（正义/中立/邪恶）

> 后续角色创建时会自动分配职业和组织归属。

### 第 5 步：角色创建

进入 **「角色工坊」**，AI 按顺序生成：
1. **主角** → 2. **配角群（3-5 人）** → 3. **反派（1-2 人）**
- 每个角色包含：姓名、外貌、性格、背景故事、目标、秘密、精神状态、实力等级、座右铭等
- 自动分配职业等级和组织归属
- 最后进行 **OOC（人设一致性）校验**，防止角色行为矛盾

### 第 6 步：逐章写作（核心循环）

进入 **「章节写作」**，这是核心创作流程：

1. **准备阶段** — AI 自动构建写作上下文（从 ChromaDB 检索相关记忆 + 大纲 + 角色信息）
2. **生成草稿** — AI 写手生成章节正文（实时流式展示）
3. **人工审核** — 查看生成内容，选择：
   - ✅ **通过** → 保存，进入下一章
   - ✏️ **润色** → 优化文笔，保持内容不变
   - 🔄 **局部重写** → 选中文字，选择策略（相似/扩展/压缩/自定义）重新生成
   - 🔁 **全文重写** → 根据反馈意见重写整章
4. **自动分析** — 章节通过后，AI 自动执行 9 维度分析：
   - 情节提取 / 伏笔检测 / 钩子提取 / 角色成长追踪
   - 情绪弧分析 / 节奏分析 / 质量评估 / 摘要生成 / 综合报告
5. 循环回到步骤 1，继续写下一章，直到所有章节完成

> 在项目列表页的「浮窗任务面板」可查看当前写作进度。

### 第 7 步：成品输出

所有章节写完后 → 进入 **「批量操作」** 可补全/调整 → 导出项目数据。

---

## 四、辅助功能

### 章节润色
选中任意段落，让 AI 在不改变情节的前提下优化文笔。支持流式实时预览。

### 多 Agent 审稿
对任意章节发起 4 个 Agent 并行审稿：
- 📖 **读者视角**：从读者体验评价
- 🧩 **逻辑检查**：检测情节矛盾和不合理设定
- ✒️ **文笔检查**：评价语言质量和风格统一性
- ⏱️ **节奏检查**：分析叙事节奏是否合理

### 伏笔管理
- 章节分析时 AI 自动检测新伏笔
- 全生命周期跟踪：**埋下 → 激活 → 回收/废弃**
- 自动分类（情节/人物/世界观/能力/情感伏笔）
- 到期预警（还剩 X 章）
- 伏笔回收率统计

### 版本对比
每章每次生成都保存为独立版本，可任选两个版本进行 Diff 对比。

### 记忆检索
- 使用 ChromaDB 向量存储所有已写章节的情节记忆
- 写作时自动 RAG 检索相关上下文，保证前后一致
- 支持语义搜索，快速查找之前的任何情节

### 批量生成
一次性批量生成多章（适用于草稿阶段），后台任务执行，支持暂停/恢复/取消。

### 封面生成
AI 根据书名+简介生成封面提示词，调用图像模型生成封面图。

### 灵感引擎
- AI 随机生成创意点子（情节转折/角色发展/世界观扩展/冲突设计/悬念设置）
- 支持反复优化
- 一键转化为新项目

### 导入书籍
上传 TXT 或 EPUB 文件，AI 自动检测章节目录、提取角色、结构化大纲。

### 更多功能
- **写作风格**：5 种内置风格（古风/轻小说/严肃文学/网文/暗黑），支持自定义
- **Prompt 模板**：可复用的提示词模板库
- **MCP 插件**：注册外部工具服务器，扩展 AI 能力
- **关系图谱**：可视化角色关系网络
- **流程图监控**：实时可视化 LangGraph 流水线执行状态
- **技能系统**：6 种内置 AI 技能，定制不同场景的写作行为

---

## 五、AI 配置

### 修改默认 AI

编辑 `.env` 文件：

```env
# 当前使用 DeepSeek（OpenAI 兼容模式）
OPENAI_API_KEY=你的DeepSeek_API_Key
OPENAI_BASE_URL=https://api.deepseek.com/v1
DEFAULT_AI_MODEL=deepseek-chat

# 也可切换模型：
# deepseek-chat / deepseek-reasoner / deepseek-v3
```

### 前端设置页

路径：侧边栏 → **「项目设置」** 或顶部导航 → 「设置」

可配置：AI 提供商 / 模型 / 自定义 API Key / Base URL / Temperature / Max Tokens / 主题外观。

支持保存多个 API 预设方案，一键切换。

---

## 六、项目结构

```
GraphNovel/
├── frontend/          # React 19 + TypeScript + Ant Design 5 前端
│   └── src/pages/     # 页面组件（每个功能一个页面）
├── backend/
│   └── app/
│       ├── agents/    # 8 个 AI Agent（写手/编辑/分析/审稿等）
│       ├── api/       # FastAPI 路由（对应每个功能模块）
│       ├── graphs/    # LangGraph 编排（主图 + 子图）
│       ├── memory/    # ChromaDB 向量记忆系统
│       ├── models/    # SQLAlchemy 数据模型（15 张表）
│       ├── services/  # 核心服务（AI 服务/封面生成等）
│       └── skills/    # AI 技能定义（SKILL.md）
├── docker-compose.yml # 服务编排
└── Dockerfile         # 多阶段构建
```
